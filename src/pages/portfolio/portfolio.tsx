import React from 'react';
import { Typography } from '@mui/material';

import './styled';

export const Portfolio = () => <Typography variant="h1">Portfolio page</Typography>;

export default Portfolio;