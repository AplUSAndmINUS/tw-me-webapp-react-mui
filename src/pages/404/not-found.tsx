import React from 'react';
import { Typography } from '@mui/material';

import './styled';

export const NotFound = () => <Typography variant="h1">Not Found page</Typography>;

export default NotFound;